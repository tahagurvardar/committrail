/** Freshness window for successful normalized public snapshots, in seconds. */
export const SNAPSHOT_REVALIDATE_SECONDS = 300;

/** Static cache namespace; repository identity is supplied as separate args. */
export const SNAPSHOT_CACHE_KEY = "phase-1a-public-repository-snapshot-v1";
