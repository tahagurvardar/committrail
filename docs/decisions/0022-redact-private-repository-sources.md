# ADR 0022: Redact private-repository sources

Private repositories use `PRIVATE_SOURCE_REDACTED`. Public objects omit repository identity, URLs, branches, SHAs, source numbers, private titles, installation metadata, and internal IDs. The page states that the source is private and not publicly accessible.
