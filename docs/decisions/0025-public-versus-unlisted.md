# ADR 0025: PUBLIC versus UNLISTED

PUBLIC projects may be indexed and appear in the profile and sitemap. UNLISTED projects remain accessible to anyone with the URL but use `noindex, follow` and are absent from indexes. Neither mode is private authentication.
